define([
    'angularAMD',
    'dirMap',
    'dirGoogleLibraries'
],
    function (angularAMD) {
        angularAMD.controller('ctrlMaps',
            function ($scope, $http, $filter) {
                var ng = $scope;


            ///
            /// Initialize function
            ///
            ng.init = function () {
            };


            ///
            ///Initialize controller
            ///
            ng.init();
        });
    });

